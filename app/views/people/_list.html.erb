<li>
  <div class="flex items-center space-x-2 my-4 mx-1 min-h-[60px]">
    <div class="w-[50px] bg-slate-600 shadow-xl border-indigo-500 rounded-full">
      <figure><img src="https://robohash.org/<%= person.id %>" class="rounded-full" /></figure>
    </div>
    <h2 class="w-full max-w-[260px] text-white px-4"><%= person.first_name %> <%= person.last_name %> <%= person.suffix %></h2>
    <div class="">
      <%= link_to(image_tag("chevron.svg", class: "max-h-[25px]"), person_path(person.id), class: "") %>
    </div>
  </div>
</li>
